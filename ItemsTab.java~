import java.awt.*; 
import javax.swing.*; 
 
public class ItemsTab extends JPanel 
{ 

   public ItemsTab() 
   { 
      //setBackground (Color.green); 
 
      JLabel l1 = new JLabel ("current items"); //replace with currentSituation.getItem().toString()
      add (l1); 
      
     //same as character info tab, have a drop menu and then buttons
   } 
} 
